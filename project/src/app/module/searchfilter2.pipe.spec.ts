import { Searchfilter2Pipe } from './searchfilter2.pipe';

describe('Searchfilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
