import { Searchfilter3Pipe } from './searchfilter3.pipe';

describe('Searchfilter3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter3Pipe();
    expect(pipe).toBeTruthy();
  });
});
