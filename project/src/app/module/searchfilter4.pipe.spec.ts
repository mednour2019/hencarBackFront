import { Searchfilter4Pipe } from './searchfilter4.pipe';

describe('Searchfilter4Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter4Pipe();
    expect(pipe).toBeTruthy();
  });
});
