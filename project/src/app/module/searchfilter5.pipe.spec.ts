import { Searchfilter5Pipe } from './searchfilter5.pipe';

describe('Searchfilter5Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter5Pipe();
    expect(pipe).toBeTruthy();
  });
});
