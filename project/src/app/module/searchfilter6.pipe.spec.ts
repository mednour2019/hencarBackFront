import { Searchfilter6Pipe } from './searchfilter6.pipe';

describe('Searchfilter6Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchfilter6Pipe();
    expect(pipe).toBeTruthy();
  });
});
